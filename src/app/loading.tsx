import * as S from '../styles/LoadingStyles';

const Loading = () => {
  return (
    <S.LoadingContainer>
      <div></div>
      <span>Apenas um momento ;)</span>
    </S.LoadingContainer>
  );
};

export default Loading;
